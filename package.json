{
  "name": "maryui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Liquid Platinum — Rao's desktop-first design system and simulated desktop.",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "node scripts/build-tokens.mjs && vite",
    "build": "node scripts/build-tokens.mjs && tsc -b --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit",
    "tokens": "node scripts/build-tokens.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
