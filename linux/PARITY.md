# Parity: web → C

`../web` is the reference. This table lists every web source file and its C counterpart in
this directory, with a status:

- **✓** same behaviour, same numbers (tests carry the same case names where the web has tests);
- **≈** ported with a deviation, named in the row and explained under *Deviations*;
- **✗** not ported, with the reason.

`make parity` (`tools/parity-check.sh`) fails when a web component lacks its C directory and
README, or when a file under `web/src/lib` or `web/src/desktop` is missing from this table.
`make gen-check` fails when the generated headers no longer match `tokens.json` / `icons.json`.

## Tokens and icons

| Web | C | Status |
|---|---|---|
| `tokens/tokens.json` | `include/maryui/lp_tokens.h` (generated by `scripts/build-tokens.mjs`, `toC` in `scripts/tokens-lib.mjs`) | ✓ every token; colours as `lp_color`, px as float, durations as `_MS`, fonts as family lists + Pango strings, shadows as `lp_shadow_layer[]`, springs as `lp_spring_params`; `LP_TOKENS[]` info table for the Gallery |
| `src/components/Icon/icons.json` | `include/maryui/lp_icons.h` (`toIconsC`) | ✓ 27 icons, `LP_ICON_PATHS` (`trash` was added for the Finder's sidebar) |
| `src/styles/tokens.css`, `src/tokens/tokens.ts` | — | generated web-side from the same source; the C side reads the header |
| `src/styles/base.css` (accent mapping, `--lp-*` motion variable defaults) | `src/core/lp_settings.c` (`lp_settings_accent`), `lp_ctx` fields `sheen_x tilt vx slosh_deg slosh_y` | ✓ |

## `src/lib`

| Web | C | Status |
|---|---|---|
| `lib/spring.ts` | `include/maryui/lp_spring.h`, `src/core/lp_spring.c`, `tests/test_spring.c` | ✓ 4/4 cases |
| `lib/slosh.ts` | `lp_slosh.h`, `src/core/lp_slosh.c`, `tests/test_slosh.c` | ✓ 4/4 cases (rim `omega *= 0.4`) |
| `lib/velocity.ts` | `lp_velocity.h`, `src/core/lp_velocity.c`, `tests/test_velocity.c` | ✓ 4/4 cases (ring of 8, 80 ms window, 60 ms stale, EMA 0.5) |
| `lib/geometry.ts` | `lp_geometry.h`, `src/core/lp_geometry.c`, `tests/test_geometry.c` | ✓ 10/10 cases |
| `lib/motionEngine.ts` | `lp_motion.h`, `src/core/lp_motion.c` (`lp_motion_engine`, `lp_window_motion`), `tests/test_motion.c`; applied in `src/compositor/window.c` | ≈ **D1 D2 D3 D4** |
| `lib/textures.ts` (feTurbulence, brush) | `src/draw/lp_noise.c` (SVG 1.1 §15.17 reference, browser stitch comparison), `src/draw/lp_texture.c` | ✓ `tests/test_noise.c` |
| `lib/brushSvg.ts` | `src/draw/lp_texture.c` (`lp_brush_tile`) | ✓ period 512/√5, rotate −atan2(1, 2), desaturate, linear transfer over #808080 |
| `lib/wallpaperSvg.ts` | `src/draw/lp_wallpaper.c` (`lp_wallpaper_render`, `lp_wallpaper_cached`, vignette) | ≈ **D5** |
| `lib/dom.ts` (`prefersReducedMotion`, `setVars`) | `lp_settings.reduced_motion` (View menu, `settings.conf`); variables are `lp_ctx` fields | ✓ no media query on Linux: the setting is the switch |
| `lib/textStats.ts` | `lp_text_doc_word_count`, `lp_text_doc_char_count`, `lp_text_doc_line_col` in `src/components/TextArea/TextArea.c`, `tests/test_text_area.c` | ✓ words by whitespace, code points, 1-based line/column |
| `lib/cx.ts` | — | class-name helper; nothing to port |

## `src/desktop`

| Web | C | Status |
|---|---|---|
| `desktop/wm/reducer.ts` | `lp_wm.h`, `src/core/lp_wm.c`, `tests/test_wm.c` | ✓ 10/10 cases: cascade 60 + 28·(i mod 8), title 28, min 240×160, zoom restores the exact rect |
| `desktop/wm/actions.ts`, `desktop/wm/types.ts` | `lp_wm_action`, `lp_window_record`, `lp_open_spec` in `lp_wm.h` | ✓ |
| `desktop/wm/selectors.ts` | `lp_wm_focused`, `lp_wm_find` | ✓ |
| `desktop/wm/store.ts`, `desktop/wm/useWM.ts` | `lp_desktop_dispatch` + `on_change` (the host syncs from the change mask) | ≈ no subscriptions: one host, one callback |
| `desktop/settings.ts` | `lp_settings.h`, `src/core/lp_settings.c` (`$XDG_CONFIG_HOME/maryui/settings.conf`) | ≈ **D6** raster wallpaper stored, not rendered |
| `desktop/menus.ts` | `lp_desktop_build_menus` in `src/core/lp_desktop.c` | ≈ **D11** plus File › New Terminal (`spawn("foot")`), a Go menu, and the focused app's entries (`lp_app.menu_entries`) |
| `desktop/apps/registry.ts` | `lp_desktop_register_builtin_apps`, `lp_app.h` (`name`, `icon`, `hidden`, `internal`, `open`, `command`, `menu_entries`, `notify`) | ≈ **D11** finder, gallery, about, textedit (hidden: Spotlight only), info (internal: opened by the Finder) |
| `desktop/spotlight.ts` | `include/maryui/lp_spotlight.h`, `src/core/lp_spotlight.c`, `lp_desktop_spotlight_*` in `src/core/lp_desktop.c`, `tests/test_spotlight.c` | ✓ 9/9 cases: the dock for a blank query, ranking (prefix, word prefix, substring), windows as items, wrap, cap 8; the C tests add the Ctrl+Space / Esc / Enter key cases |
| `desktop/SpotlightHost.tsx` | `src/compositor/spotlight.c` (one fixed-size chrome in the `z.spotlight` layer, `lp-spotlight-in` tween, hit-test inside the panel, outside-press closes) | ≈ **D9** the Terminal tile |
| `desktop/Desktop.tsx` | `src/compositor/desktop.c` (menu bar, dropdown, pointer/keyboard routing), `src/core/lp_desktop.c` (commands, keys) | ≈ opens Finder + Gallery at start like the web; hit-testing skips shadows like CSS |
| `desktop/Wallpaper.tsx` | `mui_desktop_output_ready` + `lp_wallpaper_cached` | ≈ **D5** |
| `desktop/WindowLayer.tsx` | `src/compositor/window.c` (`mui_windows_sync`) | ✓ z from the WM, focused shadow, shaded crop, zoomed radius 0 |
| `desktop/apps/AboutApp/AboutApp.tsx` | `src/apps/about.c` | ✓ monogram, `PRETTY_NAME` from `/etc/os-release` |
| `desktop/apps/FinderApp/FinderApp.tsx`, `desktop/apps/FinderApp/files.ts` | `src/apps/finder.c`, `src/apps/finder_mock.c`, `src/core/lp_files.c`, `src/apps/info.c` | ≈ **D11** a real file manager; `files.ts` lives on as the preview lp-render shows |
| `desktop/apps/GalleryApp/GalleryApp.tsx` | `src/apps/gallery.c` | ✓ five tabs in a segmented control |
| `desktop/apps/GalleryApp/tabs/ControlsTab.tsx` | `gallery.c` Controls | ✓ incl. the TextArea and Spotlight previews |
| `desktop/apps/TextEditApp/TextEditApp.tsx` | `src/apps/textedit.c` (`lp_app_textedit`) | ≈ **D10** **D11** name field, Save, TextArea, status bar, `• ` dirty marker via `SET_TITLE`, ⌘/Ctrl+S; documents are files (`~/Documents/<name>.txt` for a new one, any text file the Finder opens) and the web's are localStorage |
| `desktop/apps/GalleryApp/tabs/SurfacesTab.tsx` | `gallery.c` Surfaces | ✓ variants, liquid merge, emboss |
| `desktop/apps/GalleryApp/tabs/BubblesTab.tsx` | `gallery.c` Bubbles | ✓ |
| `desktop/apps/GalleryApp/tabs/TokensTab.tsx` | `gallery.c` Tokens from `LP_TOKENS[]` | ✓ |
| `desktop/apps/GalleryApp/tabs/MotionTab.tsx` | `gallery.c` Motion, mutating `lp_motion_live` | ≈ "Copy JSON patch" writes the patch to `~/.cache/maryui/tokens-patch.json` instead of the clipboard |

## `src/hooks`

| Web | C | Status |
|---|---|---|
| `hooks/useDrag.ts` | the MOVE grab in `src/compositor/desktop.c`; `mui_window_begin_move` | ✓ |
| `hooks/useResize.ts` | the RESIZE grab; `lp_window_handles` (6 px grips, 14 px corners) | ✓ |
| `hooks/useMotionTarget.ts` | `mui_window.target` registered with `server->engine` | ✓ |
| `hooks/useOutsideClick.ts` | a press off the dropdown and the menu bar closes it (`mui_desktop_pointer_event`) | ✓ |
| `hooks/useDesktopKeys.ts` | `lp_desktop_key` (⌘W, ⌘M, Ctrl+`, Esc, menu navigation, Ctrl/⌘+Space for Spotlight, ↑/↓/Enter while it is open; window shortcuts suspended meanwhile) | ✓ Super is ⌘ |
| `hooks/useClock.ts` | `clock_tick` in `desktop.c` (repaints only the clock's rect, once a minute) | ✓ |

## `src/components`

Every component has `src/components/<Name>/<Name>.c` and a `README.md` that follows the web
README (anatomy, variants, states, tokens) and adds a **C** section naming the header.

| Web | C header | Status |
|---|---|---|
| Surface | `components/lp_surface.h` | ✓ variants raised/flat/well/bar/titlebar/body (`LP_VARIANT_*`), brush overlay, sheen band |
| Monogram | `components/lp_monogram.h` | ✓ |
| MenuBar | `components/lp_menu_bar.h` | ✓ triggers, clock, hover switching |
| Menu, MenuItem | `components/lp_menu.h` | ≈ **D7** backdrop blur |
| TitleBar | `components/lp_title_bar.h` | ✓ title centred between 84 px insets, inactive drains |
| TrafficLights | `components/lp_traffic_lights.h` | ✓ liquid bubbles, slosh, smear |
| LiquidBubble | `components/lp_liquid_bubble.h` | ✓ swirl and slosh |
| GooGroup | `components/lp_goo_group.h` | ≈ **D8** |
| Window | `components/lp_window.h` | ✓ frame, 9-slice shadows, handles |
| Button | `components/lp_button.h` | ✓ |
| Toggle, Checkbox, Slider, TextField, SegmentedControl, ProgressBar | `components/lp_controls.h` | ✓ TextField's `large` is Spotlight's bar |
| TextArea | `components/lp_text_area.h` (`lp_text_doc`, `lp_text_clipboard`, `lp_text_area`) | ≈ **D10** |
| Spotlight | `components/lp_spotlight_panel.h` (`lp_spotlight_panel`, `lp_spotlight_measure`) | ≈ **D7** backdrop blur, **D9** |
| ScrollArea, Toolbar, Sidebar, ListRow | `components/lp_layout_components.h` | ✓ |
| Icon | `lp_icon.h` + generated `lp_icons.h` | ✓ |
| SvgDefs | `src/draw/lp_texture.c`, `src/draw/lp_draw.c`, `GooGroup.c` | ≈ the filter numbers live where they are used |

## Tests

| Web | C |
|---|---|
| `lib/spring.test.ts`, `slosh.test.ts`, `velocity.test.ts`, `geometry.test.ts`, `desktop/wm/reducer.test.ts` | `tests/test_spring.c`, `test_slosh.c`, `test_velocity.c`, `test_geometry.c`, `test_wm.c` — the same `it()` names as C identifiers |
| `desktop/spotlight.test.ts` | `tests/test_spotlight.c` — the same nine names, plus `ctrl_space_toggles_and_escape_closes` and `enter_activates_the_selection` |
| `lib/textStats.test.ts` | `tests/test_text_area.c` — `counts_words_characters_lines_and_columns`, plus the document, clipboard and layout cases the web leaves to the browser |
| `scripts/tokens.test.ts` (`toC`, `toIconsC`) | `tests/test_tokens.c`, `tests/test_icons.c` check the generated headers from the other side |
| — | `tests/test_motion.c` (engine, window motion, easing), `test_ui.c`, `test_layout.c`, `test_noise.c`, `test_smoke.c` |
| — | `tests/test_files.c` (the filesystem model), `tests/test_finder.c` (the Finder driven headlessly: navigation, selection, rename, trash, clipboard, the popup, drag and drop), `tests/test_desktop.c` (opening paths, app commands and menus, the popup, change broadcasts, TextEdit's documents) |

112 C cases in all.

## Deviations

- **D1 — drag delta.** The web keeps the frame at the store rect and translates the chrome by
  `dragDx/dragDy` every frame. The C port keeps the same fields (`lp_window_motion.drag_dx/dy`,
  `out.tx/ty`) and the compositor applies `out.tx/ty` as the subtree position, so the pixels
  match; the WM commits `MOVE` on release like `onEnd`.
- **D2 — skew.** `skewX(--lp-jelly-skew)` has no equivalent in `wlr_scene` (translate and scale
  only). `out.skew_deg` is computed for parity and not applied.
- **D3 — clients keep their pixels.** The jelly scale is applied to built-in windows only
  (`out.jelly_sx/sy`); a Wayland client's frame is not stretched, because its content cannot
  be. During a FLIP flight the frame scales and the client tree is repositioned and clipped
  (`wlr_scene_subsurface_tree_set_clip`) to the scaled body; the content is not resampled.
- **D4 — where the sheen moves.** On the web `--lp-sheen-x` is inherited by every Surface in
  the window. In C the moving sheen and tilt repaint the title bar strip only (the lights and
  the title's band); body surfaces keep a static sheen until they repaint for another reason.
  Repainting a whole body with Cairo under pixman would not hold 60 fps at 1280×800.
- **D5 — wallpaper drift.** The wallpaper's slow drifting highlight (`motion.drift-period`) is
  not ported: it would be a full-screen blend every frame. The filter chain, the vignette and
  the prerendered `wallpaper-WxH.png` cache are.
- **D6 — raster wallpaper.** View › Raster Wallpaper is stored in `settings.conf` and shown as
  checked, but the procedural wallpaper is still rendered (there is no `platinum.jpg` in the
  image yet).
- **D7 — menu backdrop.** `backdrop-filter: blur(14px) saturate(1.1)` is not available to a
  scene node; the dropdown is the 94 %-alpha surface alone.
- **D8 — liquid merge.** GooGroup's `feGaussianBlur` + `feColorMatrix` alpha contrast on blank
  blobs (xs 1.6/20/−8, sm 3/18/−7, md 6/19/−9) is applied per group when painted; the smear
  with `--lp-vx` is ported; the hover "reach" is static.
- **D9 — the Terminal tile.** Spotlight lists `Terminal` as a command on both sides. In C it runs
  `LP_CMD_NEW_TERMINAL` (`spawn("foot")`) and the dock's dot lights while a client window is
  open; on the web it is a no-op — the browser has no processes.
- **D10 — TextArea and TextEdit.** The web TextArea is a native `<textarea>`: the browser owns
  the caret, the selection, the clipboard and key repeat, and TextEdit saves to `localStorage`.
  The C TextArea owns a UTF-8 document (`lp_text_doc`) with a caret and an anchor, wraps through
  Pango (`lp_text_layout_*` in `lp_text.h`, built from a scratch image context in the EVENT pass
  so hit-testing agrees with drawing), keeps a process-wide clipboard (`lp_text_clipboard_shared`,
  no `wl_data_device` yet, so nothing crosses to Wayland clients), and the compositor repeats a
  held key for chromes with the seat's rate and delay (`key_repeat` in `input.c`). TextEdit writes
  `~/Documents/<name>.txt`. Undo is on neither side yet.
- **D11 — the Finder is real.** The web Finder browses `files.ts`, a mock with no nesting, and
  its TextEdit keeps documents in `localStorage`; the two never meet. On Linux the Finder is a
  file manager over the guest's filesystem (`lp_files.h`: listing, kinds, Finder-style sizes and
  dates, natural sort, the user's folders, volumes from `/proc/mounts`, new folder, rename,
  freedesktop.org Trash, copy with “x copy” names, move, a file clipboard) with history,
  keyboard navigation, inline rename, a context menu, an Info window, drag and drop between
  windows (`lp_drag.h`, the compositor's `drag.c`) and inotify refresh (`files.c`); TextEdit
  opens and saves real files. The apps gained `open`, `command`, `menu_entries` and `notify`
  hooks, the desktop a Go menu, `LP_CMD_APP`, `lp_desktop_open_path`, a popup slot and a drag
  session; none of it is on the web, which stays the design team's component reference.
  `lp-render --finder` keeps painting the mock (`finder_mock.c`) so the renders compare.
- **Close animation.** `lp-window-close` (scale .96 + fade over `motion.fast`, `CLOSE` after
  fast + 80 ms) runs for built-in windows. A client that unmaps is gone at once — the compositor
  has no pixels left to fade.
- **Frame pacing.** The web's `requestAnimationFrame` is vsync. A virtual GPU under pixman
  completes page flips immediately, so `output.c` paces frames to the output's refresh
  interval with a timer and ignores flips that complete early unless a frame was asked for.
  Without a hardware cursor (virtio-gpu in Virtualization.framework) wlroots draws a software
  cursor and flags `needs_frame` after every commit; a frame that would only re-commit the
  same picture (no damage, no motion, pointer still) is skipped, so an idle desktop draws
  nothing. Ambient animations (`lp_want_frame_rect`: the indeterminate progress bar's barber
  pole and glint, the Bubbles tab's swirl) repaint their own rectangle at 30 Hz while they are
  on screen, the way a CSS animation keeps running on the web; the engine's semantics (`wake`
  once, tick while active, idle) are the web's.

## Constants that are not tokens yet

These numbers are the same on both sides but live in code, not in `tokens.json`. Promoting
them is the follow-up; each row names both homes.

| Constant | Web | C |
|---|---|---|
| Goo filter (blur, alpha slope, alpha intercept) xs / sm / md | `SvgDefs.tsx` | `GooGroup.c` |
| Wallpaper gradient stops, turbulence, blur, lighting, arithmetic | `wallpaperSvg.ts` | `lp_wallpaper.c` |
| Brush period 512/√5, angle −atan2(1, 2), desaturation, transfer | `brushSvg.ts` | `lp_texture.c` |
| Toolbar height 40, sidebar width 180 | `Toolbar.module.css`, `Sidebar.module.css` | `LP_TOOLBAR_H`, `LP_SIDEBAR_W` |
| List row 22, list header 20 | `ListRow.module.css` | `LP_LIST_ROW_H`, `LP_LIST_HEADER_H` |
| Menu min width 200, menu shadow extent 40, anchor 2 px below the bar | `Menu.module.css`, `MenuBar.tsx` | `LP_MENU_MIN_WIDTH`, `LP_MENU_SHADOW_EXTENT`, `sync_menu_chrome` |
| Cascade 60 / 28, title 28, default window 640×440 | `reducer.ts` | `lp_wm.c` |
| PointerTracker: 8 samples, 80 ms window, 60 ms stale, EMA 0.5 | `velocity.ts` | `lp_velocity.h` |
| Double click 400 ms | `TitleBar.tsx` | `DOUBLE_CLICK_MS` in `desktop.c` |
| Finder: tile 96×88, status bar 22, path bar 22, drag threshold 4 px, type-ahead 1 s, ghost 120×84 | — (Linux only) | `finder.c`, `lp_drag.h` |
| Spotlight: bar centre at 38 % of the desktop height, dock cell 88×84, 28 px tile icon, 8 results, shadow extent 40 | `spotlight.ts`, `Spotlight.module.css` | `LP_SPOTLIGHT_Y_FRACTION`, `LP_SPOTLIGHT_CELL_W/H`, `LP_SPOTLIGHT_MAX_RESULTS`, `LP_SPOTLIGHT_SHADOW_EXTENT` |
| TextArea padding `space.2`, 10 px scrollbar lane, ⌘/Ctrl+S in TextEdit, `• ` dirty marker | `TextArea.module.css`, `TextEditApp.tsx` | `TextArea.c`, `textedit.c` |
| CLOSE_MS = fast + 80, SHADE_MS = slow, settle tolerances of the springs | `Window.tsx`, `motionEngine.ts` | `window.c`, `lp_motion.c` |
| Slosh → `--lp-slosh-y` factor 6 px, `--lp-slosh` sign | `motionEngine.ts` | `lp_motion.c` |
