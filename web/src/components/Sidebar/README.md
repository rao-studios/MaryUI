# Sidebar

A source list: `SidebarSection` headers (uppercase tertiary ink) and `SidebarItem` rows (icon in accent; selected row takes the accent gradient).

## Tokens
`--lp-surface-sidebar`, `--lp-edge-divider`, `--lp-accent-*`, `--lp-radius-sm`, `--lp-text-xs/md`, `--lp-ink-*`.
