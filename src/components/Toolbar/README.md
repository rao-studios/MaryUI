# Toolbar

The 40px brushed strip under a title bar. `ToolbarGroup` clusters controls; `ToolbarSpacer` pushes the rest right.

## Tokens
`--lp-surface-window-*`, `--lp-edge-light`, `--lp-edge-hairline`, `--lp-space-2/-3`.
