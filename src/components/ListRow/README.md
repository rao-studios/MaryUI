# ListRow

One 22px line of a list view: icon, name, trailing columns. Even rows tint; the selected row takes the accent gradient. `ListHeader` is the sticky column header.

## Tokens
`--lp-accent-*`, `--lp-platinum-1/-2`, `--lp-edge-divider`, `--lp-text-xs/sm`, `--lp-ink-*`.
